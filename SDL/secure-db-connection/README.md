# Задание №1
Что было сделано на первом семинаре:
1 Установить Postgresql 16 / 17 (варианты на ОС / docker).
2 Создать базу данных c настройками Lang = ru_RU.UTF-8,
аутентификация scram-sha-256 (по умолчанию).
3 Создать в пользователя с доступом USAGE к этой БД и
схеме public в ней.
4 Разработать приложение на любимом языке
программирования, удовлетворяющее следующим
требованиям:
Подключаться к БД и выполнять sql запрос:
SELECT VERSION();
Что требуется доделать:
Разработанное приложение должно:
1 Считывать настройки подключения к БД лежать рядом в
файле, например *.conf или *.json, *.yaml, *.toml.
2 Получать от пользователя логин и пароль для
подключения к БД, консоль / графически интерфейс.
3 Каким либо образом безопасно соединять данные из
файла и полученные от пользователя, там образом, чтобы
пользователь своим вводом не мог добавить своих опций в
параметры подключений к БД.
